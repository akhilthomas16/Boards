[pytest]
DJANGO_SETTINGS_MODULE = myproject.settings
python_files = tests.py test_*.py
# ponytail: accounts/ and boards/ tests are excluded — they need CREATEDB on the
# Postgres role and only cover the Django template views Phase 1 deletes.
# Run them explicitly with: pytest accounts boards
testpaths = api cms
